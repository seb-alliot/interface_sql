IMPORTANT :

- Ce fichier `env` doit être renommé en `.env` sur le serveur local (ou machine locale) pour que l’application fonctionne.

- Ce fichier ne devras pas être partager une fois l'application configurer.

